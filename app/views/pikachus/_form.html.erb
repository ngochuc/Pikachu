<%= form_with(model: pikachu) do |form| %>
  <% if pikachu.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pikachu.errors.count, "error") %> prohibited this pikachu from being saved:</h2>

      <ul>
        <% pikachu.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
